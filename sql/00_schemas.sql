-- Create the target schemas (one PostgreSQL schema per Oracle sample schema).
-- Safe to re-run.
CREATE SCHEMA IF NOT EXISTS hr;
CREATE SCHEMA IF NOT EXISTS co;
CREATE SCHEMA IF NOT EXISTS sh;
CREATE SCHEMA IF NOT EXISTS scott;
